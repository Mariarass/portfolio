import React from 'react';
import s from './Clue.module.css'

const ClueComponent = () => {
    return (
        <div className={s.container}>
            Enter
        </div>
    );
};

export default ClueComponent;
